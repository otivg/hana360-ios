<?xml version="1.0" encoding="utf-8"?>
<!-- FileProvider scope for the in-app updater: only the cache dir,
     where downloaded release APKs land. -->
<paths>
    <cache-path name="updates" path="." />
</paths>
